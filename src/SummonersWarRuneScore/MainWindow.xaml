<Window x:Class="SummonersWarRuneScore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
		mc:Ignorable="d"
        Title="Summoners War Rune Score" Height="960" Width="1280" Loaded="Window_Loaded">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="50*" />
			<RowDefinition Height="5" />
			<RowDefinition Height="50*" />
		</Grid.RowDefinitions>

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="50*" />
				<ColumnDefinition Width="25*" />
				<ColumnDefinition Width="25*"/>
			</Grid.ColumnDefinitions>

			<Grid.RowDefinitions>
				<RowDefinition Height="22" />
				<RowDefinition />
				<RowDefinition Height="22" />
			</Grid.RowDefinitions>

			<ComboBox Name="cbxRuneSet" Grid.ColumnSpan="1" SelectionChanged="cbxRuneSet_SelectionChanged" />
			<ListView Name="lvMonsterRoles" Grid.Row="1" Grid.RowSpan="2" SelectionChanged="lvMonsterRoles_SelectionChanged" />

			<Button Name="btnAdd" Content="Add New Role" Grid.Column="1" Grid.ColumnSpan="2" Click="btnAdd_Click" />

			<ScrollViewer Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition />
						<ColumnDefinition />
					</Grid.ColumnDefinitions>

					<StackPanel>
						<Label Content="HP%:" Target="{Binding ElementName=tbHpPercent}" />
						<xctk:DecimalUpDown Name="tbHpPercent" Text="{Binding Path=SelectedMonsterRole.HpPercentWeight}" />
						<Label Content="ATK%:" Target="{Binding ElementName=tbAtkPercent}" />
						<xctk:DecimalUpDown Name="tbAtkPercent" Text="{Binding Path=SelectedMonsterRole.AtkPercentWeight}" />
						<Label Content="DEF%:" Target="{Binding ElementName=tbDefPercent}" />
						<xctk:DecimalUpDown Name="tbDefPercent" Text="{Binding Path=SelectedMonsterRole.DefPercentWeight}" />
						<Label Content="Expected Base HP:" Target="{Binding ElementName=tbExpectedBaseHp}" />
						<xctk:DecimalUpDown Name="tbExpectedBaseHp" Text="{Binding Path=SelectedMonsterRole.ExpectedBaseHp}" />
						<Label Content="Expected Base ATK:" Target="{Binding ElementName=tbExpectedBaseAtk}" />
						<xctk:DecimalUpDown Name="tbExpectedBaseAtk" Text="{Binding Path=SelectedMonsterRole.ExpectedBaseAtk}" />
						<Label Content="Expected Base DEF:" Target="{Binding ElementName=tbExpectedBaseDef}" />
						<xctk:DecimalUpDown Name="tbExpectedBaseDef" Text="{Binding Path=SelectedMonsterRole.ExpectedBaseDef}" />
					</StackPanel>
					<StackPanel Grid.Column="1">
						<Label Content="SPD:" Target="{Binding ElementName=tbSpd}" />
						<xctk:DecimalUpDown Name="tbSpd" Text="{Binding Path=SelectedMonsterRole.SpdWeight}" />
						<Label Content="Cri Rate:" Target="{Binding ElementName=tbCritRate}" />
						<xctk:DecimalUpDown Name="tbCritRate" Text="{Binding Path=SelectedMonsterRole.CritRateWeight}" />
						<Label Content="Cri Dmg:" Target="{Binding ElementName=tbCritDmg}" />
						<xctk:DecimalUpDown Name="tbCritDmg" Text="{Binding Path=SelectedMonsterRole.CritDmgWeight}" />
						<Label Content="Resistance:" Target="{Binding ElementName=tbResistance}" />
						<xctk:DecimalUpDown Name="tbResistance" Text="{Binding Path=SelectedMonsterRole.ResistanceWeight}" />
						<Label Content="Accuracy:" Target="{Binding ElementName=tbAccuracy}" />
						<xctk:DecimalUpDown Name="tbAccuracy" Text="{Binding Path=SelectedMonsterRole.AccuracyWeight}" />
					</StackPanel>
				</Grid>
			</ScrollViewer>

			<Button Name="btnDelete" Content="Delete" Grid.Column="1" Grid.Row="2" Click="btnDelete_Click" />
			<Button Name="btnSave" Content="Save" Grid.Column="2" Grid.Row="2" Click="btnSave_Click" />
		</Grid>

		<GridSplitter HorizontalAlignment="Stretch" Grid.Row="1" />

		<DockPanel Grid.Row="2">
			<Button Name="btnImport" Content="Import Runes" Click="btnImport_Click" DockPanel.Dock="Top" />
			<DataGrid Name="dtGrdRunes" IsReadOnly="True" FontSize="14" DockPanel.Dock="Bottom" />
		</DockPanel>
	</Grid>
</Window>
